#include "Pilha_padrao.h"
#include<stdio.h>
#include<stdlib.h>

int main(){
/*
	Pilha p;
	int elem, n, x;

    p=cria_pilha();
    push(p,1);
    push(p,2);
    push(p,3);
    push(p,4);
    printar(p);
    pop(p,&x);
    printf("elemento removido = %d\n",x);
    push(p,5);
    printar(p);


*/
    int num;
    scanf("%d",&num);
    converte(num);

/*	while(1){

		printf("escolha um caso:\n 1- criar lista\n 2- inserir elemento\n 3- remover topo e imprimir removido\n 4- imprimir lista\n");
        scanf("%d", &x);
	    printf("\n\n");
		switch(x){
		case 1:
			p = cria_pilha();
			break;
		case 2:
			printf("insira um numero: \n");
			scanf("%d", &n);
			push(p, n);
			break;
		case 3:
			pop(p, &elem);
			printf("REMOVIDO: %d\n", elem);
			break;
		case 4:
			printar(p);
			break;
		default:
			printf("FIM\n");
			return 0;
	}
}
*/




return 0;
}
